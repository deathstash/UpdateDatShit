A simple bash script I wrote to make updating and upgrading a linux system faster, should work on most debian/ubuntu based distros.
To install give executible permissions and run ./UpdateDatShit.sh.
Script will install all dependencies prior to executing.
